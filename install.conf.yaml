- clean: ['~']

- link:
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc


- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.vimrc: vimrc


